# Supervised Fine-Tuning Configuration — Software Engineering Specialist
stage: sft

base_model: "./outputs/pretrain/best_model"

data:
  processed_dir: "./data/processed"
  train_file: "sft_train.jsonl"
  val_file: "sft_val.jsonl"
  max_seq_length: 8192
  prompt_field: "prompt"
  response_field: "response"
  train_on_prompt: false

# SE-specific prompt template — structured, production-oriented output
prompt_template: |
  <|im_start|>system
  You are an expert software engineer. Your sole focus is software engineering, system design, and application development.
  <|im_end|>
  <|im_start|>user
  {instruction}
  <|im_end|>
  <|im_start|>assistant

response_template: "<|im_start|>assistant\n"

training:
  output_dir: "./outputs/sft"
  num_train_epochs: 3
  per_device_train_batch_size: 2
  per_device_eval_batch_size: 2
  gradient_accumulation_steps: 16
  learning_rate: 5.0e-6
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.03
  weight_decay: 0.01
  max_grad_norm: 1.0
  bf16: true
  fp16: false
  tf32: true
  gradient_checkpointing: true
  dataloader_num_workers: 4
  dataloader_pin_memory: true
  logging_steps: 10
  eval_steps: 200
  save_steps: 200
  save_total_limit: 3
  evaluation_strategy: "steps"
  load_best_model_at_end: true
  metric_for_best_model: "eval_loss"
  greater_is_better: false
  report_to: ["wandb", "tensorboard"]
  run_name: "se-llm-sft"
  seed: 42

packing: false

deepspeed_config: "./config/ds_zero2.json"
